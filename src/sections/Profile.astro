---
import GithubProfileCard from '../components/GithubProfileCard.astro'
import type { GHProfile } from '../types/github'

interface Props {
  prof: GHProfile
}

const { prof } = Astro.props
---

<div class="flex flex-col gap-2 md:gap-4" id="contact">
  <GithubProfileCard prof={prof} />
</div>
